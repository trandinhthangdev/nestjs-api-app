version: '3.8'
services:
  dev-database:
    image: 'postgres:15.0'
    ports:
      - 5434:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345678
      POSTGRES_DB: testdb
    networks:
      - nestjs-networks
networks:
  nestjs-networks:

# docker compose up -d 